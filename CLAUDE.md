# ServiFlow-AI

交易分类流水线：多个分类引擎按优先级顺序执行，对交易逐行分类并输出 Excel 报告（`backfill.py`）或单应用 JSON（`verify_model.py`）。

## 引擎覆盖行为

- **执行顺序**：由 `configs/pipeline.json` 中每台引擎的 `priority` 决定，**按 priority 升序执行**（见 `classification_core/config.py` 的 `PipelineConfig.enabled_engines`），不是按 JSON 数组中的书写顺序。当前顺序（priority）：transfer(1) → initial(10) → dishonour(150) → gambling(180) → income(200) → liability(300) → all_other_credit(400) → fee(500) → rent(800) → catch_all(999)。
- **覆盖规则**：所有引擎都看到全部交易（`candidates` 默认全量）。每台引擎跑完后，其结果在行级写入 `output`，**覆盖**该行已有的 `bscat` 与 `counterparty`（成对覆盖），并记录 `classification_engine` / `classification_priority` 等字段。**后面的引擎永远赢**（覆盖写入点：`classification_core/orchestrator.py` 的 `_commit`），唯一例外见**特例 5**：gambling 认领的行对 income / liability 不可覆盖。
- **特例 1（income 保护）**：`liability` 引擎的候选集会排除已被分为收入类的交易（`orchestrator.py` 的 `run`），因此不会覆盖这些行。排除判定基于**当前输出层的 `bscat`**，即收入引擎映射后的粗类 `Wages` / `Centrelink`（`salary_payg` / `salary_packaging` / `self_employed_gig` 三者在 `income_engine/domain/classification.py` 的 `add_income_type_rules` 中统一映射为 `Wages`）。
- **特例 2（all_other_credit）**：`all_other_credit` 只处理 `dr_cr == credit` 的行，且跳过已被先前引擎分类的行——唯一例外是 `External Transfers` 允许被它重新匹配（`all_other_credit_engine/engine.py` 的 `classify`）。
- **特例 3（catch_all）**：`catch_all` 只匹配未被先前引擎分类的行（`catch_all_engine/engine.py` 的 `classify`）。
- **特例 4（rent）**：`rent` 引擎的候选集排除已被 income / liability 分类的行（`orchestrator.py` 的 `run`）。引擎内部分两层：机构层（`rent_rules.csv` 中 `source=institution` 的行，Aho-Corasick 匹配，counterparty=商户名）**跳过已被 fee / dishonour 认领的行**，且**跳过已被 initial 认领且其匹配关键词长度 ≥ 本次 rent 命中关键词长度的行**（从 initial 认领的 `classification_reason` 中解析 `keyword=K` 的长度）——这两条是为保持 2026-09 机构迁移前的赢家语义（当时机构在 initial(10)，跨类别最长关键词胜，fee/dishonour 可覆盖）；关键词层（`source=rule` 的行）仍可认领任何行。机构层命中时赢过关键词层（confidence 0.95 > 规则 ≤0.90），counterparty 用商户名。机构层关键词**不做静态过滤**（全量 21,794 机构原样迁入），跨类别冲突全部由运行时的长度比较解决。
- **特例 5（gambling）**：`gambling` 引擎 **priority 180**（dishonour 之后、income/liability 之前，2026-09 用户确认前移；原为 700 且候选集排除 income / liability，现顺序反转后该排除对 gambling 已是空操作，代码保留作防御性守卫——**若未来把 gambling 移回 income/liability 之后，必须重审该守卫与本特例的保护过滤**）。**gambling 认领的行对 income(200) / liability(300) 不可覆盖**（实现：`orchestrator.py` 的 `run` 中 validate 之后、`_archive_claims` 之前，按 key 丢弃 income/liability 对 `classification_engine=="gambling"` 行的预测——放在提交层而非候选集，因 income 的行为特征在全量行上计算，候选过滤会扭曲其余行特征）；fee / rent / catch_all 等其他后续引擎的 later-wins 语义不变（2026-09 全量样本实测：fee 双命中行仍归 fee，仅 fee 的 `unclassified_only` catchall 会向 gambling 让路）。机构层语义同 rent（跳过 fee / dishonour + initial 长度让路，共享代码 `classification_core/merchant_institution.py`；fee 分支在 180 时无 fee 认领、成空操作）；**关键词层与 rent 相反——排除所有先前引擎已认领的行**（`exclude_prior_claimed`，复刻迁移前 Gambling 规则在 catch_all(999) 时只处理未认领行的语义；180 时存在的先前认领只可能来自 transfer / initial / dishonour）。机构层命中时赢过关键词层，counterparty 用商户名；关键词层 counterparty 为 "-"。配置表 `gambling_engine/resources/gambling_rules.csv`（结构同 rent_rules.csv）：16 条规则（原 catch_all 的 POKIES/WAGERING/CASINO 等）+ 1,822 机构行（原 merchant_kb Gambling 类）原样迁入。
- `priority` 仅决定执行顺序，并记录进 `classification_priority` 列，不参与覆盖判定（除特例 5 的 gambling→income/liability 保护依赖 180 < 200/300 这一配置事实外）。

## 入口与数据流

**两套出入参契约，同一入口自适应**（实现与派发都在 `classification_core/service.py`：v1 构建/序列化为 `build_transactions_frame` / `serialize_result`，v2 为 `build_wagego_frame` / `serialize_wagego`，派发入口 `build_input_frame` / `serialize_output` / `build_failed_output`）。**派发规则：`product` 优先，按键判定兜底**——payload 带非空字符串 `product` 时查产品表（大小写不敏感、两侧空白忽略）：`fundo` → v1、`wagego` → v2；**未注册的取值 → failed**（`build_input_frame` 抛 `Unsupported 'product' value ...`，failed 出参的 `bscat_stats` 回显该原值）。`product` 缺失 / 空串 / 非字符串 → 回退按键判定：有 `illion_raw_transactions` → v1（两个键都在时 v1 优先，保证 v1 行为零变化）；否则有 `raw_transactions` → v2；两者都无 → 走 v1（错误文案与旧版逐字一致）。产品表在 `service.py` 的 `PRODUCT_FORMATS`（加产品 = 加一行）；`bscat_stats.product` 回显 payload 的 `product` 原值（不转大小写），缺省时回填解析出的契约对应产品名（fundo / wagego）。**v1 的构建路径（`build_transactions_frame`）2026-09-18 做过健壮性加固——重复 `bank_account_id` 不再扇出行、`bank_accounts` 缺失/为空/非 list 不再让 liability 无保护读 `bank` 报 KeyError、交易对象缺 `text` / `dr_cr` / `amount` / `transaction_date` / `bank_account_id` 也不再整批崩（详见下面 v1 条）；除此之外行为不变（健康报文出参逐字节相同）。出参 `bank_accounts` 同期改为**一账号一条**：两个契约共用 `_account_dicts_by_key(payload, key_of)`（重复键折叠为**最后一条**、位置取首次出现、无键/NaN/空键的条目丢弃），帧查表与出参回显同源同一份映射，不会互相打架。出参契约 2026-09 统一改过，见下面「出参契约」条。**

- **健壮性兜底（2026-09-18 第二批，A–D，与两条生产报错同一批加固的延续）**：
  - **(A) 失败兜底**：`ModelService.predict` 现在把三类失败都收成 failed 出参，永不外抛——① 请求体不是 JSON 对象（`[1,2]` / `"junk"` / `null`）→ `error = "Input payload must be a JSON object."`（无回显键可取，`bscat_stats` 按 v1 形状）；② 构建期异常（契约不符、未注册 product、v2 无可用 applicationId——都在 `build_input_frame` 内抛）→ failed 出参，不记日志；③ **`orchestrator.run` / `serialize_output` 异常 → `logging.exception("classification run failed")` 记完整 traceback 后返回 failed 出参**——`_prepare_input` 的三类硬报错（重复 `(application_id, transaction_id)` / 空键 / 缺 `transaction_id`）是在 `orchestrator.run` **内部**抛的，走的就是这一支，**同样会记 traceback**（2026-09-18 实测确认；当初写这批文档时误记为 ②）。③ 以前是直接穿到 HTTP 500，且调用方**无法区分「引擎/序列化 bug」与「服务挂了」**。因此 failed 出参不再只表示入参不合法；`verify_model.py` / `model_main.py` 走各自的语义（verify 仍外抛，生产走 predict → 见上）。**引擎里若新增「读到脏值就 raise」的行为，现在会变成整批 failed 而不是 500**——排障看服务日志里的 `classification run failed`。
  - **(B) 类型守卫**：`classification_core/text.py` 新增 `is_missing_value(value)` = `not pd.api.types.is_scalar(value) or pd.isna(value)`，`clean_text` / `clean_text_with_channel_prefix` / `clean_text_with_seams` / `parse_decimal_amount` 与**三处引擎本地 `normalize_text`**（`transfer_engine/domain/classification.py`、`fee_engine/domain/classification.py`、`liability_engine/domain/summary.py`）以及 liability `streams.py` 的 `normalize_amount_key` / `normalize_group_value`（这两个按 `.map()` 吃帧里的 `amount` / 账号列）全部改用它。理由：`pd.isna(["a","b"])` 返回的是**数组**（不是布尔），`if pd.isna(value):` 一判就抛 `ValueError: The truth value of an array with more than one element is ambiguous`——JSON 数组型字段进引擎的通用雷；非标量一律按缺失处理（**不 str() 出内容**，避免 payload 内容混进关键词匹配）。规则 CSV 加载路径（`counterparty.py` 的 `normalize_rule_value` 等）只吃 CSV 单元格，不在覆盖面内。
  - **(C) 边界归一化（必读列只能是标量）**：两个 builder 都在补齐必读列后做两件事——`text` 过 `_to_text_column`（元素级 `_as_text`：数字 `text` → 其字符串形式 `12345` → `"12345"`，数组 / 对象 `text` → `""`，None/NaN → `""`，**不 str() 出内容**以免 payload 内容混进关键词匹配）；其余必读列（`dr_cr` / `amount` / `transaction_date` / `bank_account_id`）过 `_to_scalar_column`（数组 / 对象 → 该列的缺失默认值，标量原样保留）。**整列已全为字符串 / 全为标量时原 Series 原样返回**（健康报文逐字节不变）。v2 的 `bank_account_number` **不再是恒补的必读列**（2026-09-18 对齐官方样例时移出，见 v2 条）：它是上游字段，数组 / 对象取值由账户关联的类型守卫（`_wagego_row_account_number`）按「该行无账号」处理，行内仍原样回显。根因有三处：`Series.str.upper()` 对非字符串产出 NaN，而 **NaN 是 Python 真值**，引擎里的 `if text:` 守卫会放它过去，`len(NaN)` 打死整批（`initial_engine` 的 `_clean_transaction_text` → `_classify_one`）；数组 `amount` 在 liability 的 `normalize_amount_key`（`streams.py`）炸 `ValueError: truth value of an array`；数组 `bank_account_id` 在 orchestrator 的键唯一性检查炸 `unhashable type: 'list'`（`.duplicated()` 给整列做哈希，**一行是数组就够**）。CSV 路径（`backfill.py` / `baseline.py`）读进来必然是字符串或 NaN，`fillna("")` 已覆盖，故未动引擎内那几处向量化清洗。
  - **(D) v1 账户键类型宽容**：行侧与账户侧的 `bank_account_id` 都过 `_illion_account_id`（= `normalize_account_number`）规范化后再查表，`5116090` / `5116090.0` / `"5116090"` 视为**同一账户**（此前字符串行 id 配 int 账户 id 会**静默**查不到 → `account_type` / `bank` / `credit_limit` 三列全空 → 依赖 `bank` 的 liability 规则集体失效，不报错）；**帧里的行 id 与出参回显都保留各自的原值**（int 还是 int），只有查表用规范键；前导零不合并（`"0511"` ≠ `511`）。
  - 同期收口的两处序列化 / 统计：`_max_date`（`bscat_stats.transaction_date_max`）只采信**字符串**日期，null / 数字 / 数组一律忽略、全不可用则为 `null`（原写法 `max()` 混 str 与数字会 `TypeError`）；`_serialize_value` 对**非标量**按数组回显（`value.tolist()`），不再让 `pd.isna(list)` 在成功分类之后炸掉整个出参。
- **v1 / illion（`product: fundo`）**：`userId / applicationId / flowTime / bank_accounts / illion_raw_transactions`；交易自带 `transaction_id`(int)，账户键为 `bank_account_id`(int)，账户元数据 `account_type` / `bank` / `credit_limit` 由 `bank_accounts` **dict 查表**（同 v2，**非 pandas merge**，2026-09-18 修）按 `bank_account_id` 取，查不到（或 `bank_accounts` 缺失 / 为空 / 非 list）则补默认值 `""` / `""` / `None`——这三列必须存在，liability 的 counterparty 规则**无保护**读 `bank`、其 summary 无保护读 `account_type` / `credit_limit`；空 `bank` 不命中任何规则（`credit_card_rules.csv` 每行要么是具体银行、要么是 `*` 跳过哨兵，没有空值）。（都是内部列；`credit_limit` 不再出现在出参里。）**2026-09-18 健壮性加固（与 v2 对齐）**：帧必须带齐引擎**无保护**读取的列——`text` / `dr_cr` / `amount` / `transaction_date` / `bank_account_id`（`_MANDATORY_DEFAULTS` + `_ILLION_MANDATORY_DEFAULTS`），交易对象缺哪一列就补默认值（`""` / `""` / `NaN` / `None` / `None`），不再整批 KeyError。`amount` 的缺失值用 `NaN` 而非 `None`：liability 的 `amount_gt` 阈值（`home_loan_car_loan_rules.csv` 有 1 条 `amount_gt=0`）要与金额列比较，而 `Decimal` 与缺失值比较会抛 `decimal.InvalidOperation`——**只要有一行缺金额**（不只是整列缺失）旧代码就会打死整批，已改为按 float 比较（`liability_engine/domain/counterparty.py` 的 `_get_all_rules_mask`）。出参 `bank_accounts` 不逐条回显，而是按 `bank_account_id` 去重后逐账号一条（`_account_dicts_by_key`，见上条）。查表键由 `_illion_account_id`（= `normalize_account_number`）规范化，行侧与账户侧同账号的不同类型写法（int / float / str）都能匹配上，回显与帧里仍各自保留原值（见上「健壮性兜底」D）；`text` 列在构建期归一化为字符串（见上「健壮性兜底」C）。
- **v2 / wagego（`product: wagego`）**：`raw_transactions` + `bank_accounts`（**可能整键缺失**，线上报文就没有）；交易无 id、账户无 id。**2026-09-18 按官方样例（`output/wagego_payload_sample_input.json` / `..._output.json`）对齐**：① 行与账户的关联按**行自带的账号**——线上形态是 `bank_account_number`（通常另带行级 `institution`），官方样例形态只有 `account_number`（无行级 institution），此时从命中的账户条目取 `institution`（缺失则取该条目的 `bank`）；**显式带 institution 的行只按「institution + 账号」匹配**，两家银行同名账号不会互借元数据（`_wagego_row_account` / `_wagego_row_account_number`：账号先取 `bank_account_number`、再取 `account_number`，非标量按「无账号」）。② 合成为内部列：`application_id` = `applicationId` 非空否则 `flowId`（两者都空则 failed）；`transaction_id` = 数组行号（1-based）；`bank_account_id` = `f"{institution}-{number}"`（规范化后拼接，institution 前缀恒定使内部排序与直接排账号串一致)；`bank` = 上一步解析出的 institution（liability 的 counterparty 规则按 `bank` 建掩码，**必须映射**）；`account_type` / `credit_limit` 用 dict 查表从 `bank_accounts` 取（**非 pandas merge**——重复账号会扇出行、破坏 `(application_id, transaction_id)` 唯一性；查表与出参回显共用 v1 那份 `_account_dicts_by_key`，重复账号折叠为最后一条、无键条目丢弃；账号唯查另有一份按**裸账号**建的映射 `_wagego_account_number_key`，供无 institution 的行使用），查不到则 `""` / `None`。③ **六个合成列一个都不出现在出参行里**（`_WAGEGO_ROW_EXCLUDE` 含 `transaction_id` / `bank_account_id` / `application_id` / `bank` 及账户元数据三列）：v2 行 = **上游字段按原顺序 + `counterparty` / `bscat` / `stream_id`**，且上游值**原样回显——`""` 仍是 `""`、null 仍是 null、数组仍是数组**（实现：`serialize_wagego` 把 `_payload_row_columns(payload)` 作为 `keep_empty_columns` 传给 `_serialize_records`，只有管线自己产出的列才继续把空串归一成 null；**v1 不受影响**，其 `""` 仍出 null）；`day_end_balances` 与 80+ 特征键忽略。④ 出参 `bank_accounts` 回显改成**固定 8 键**（`_WAGEGO_ACCOUNT_KEYS`，`bank` 插在 `account_number` 之后，与官方样例同序）：条目没送的键出 null。⑤ **不再给行补 `bank_account_number: ""`**（原 `_WAGEGO_MANDATORY_DEFAULTS` 已删——没有引擎读这一列）。`text` 与 v1 一样在构建期归一化（见上「健壮性兜底」C）；账号规范化的类型宽容（int / float / str 同账号）覆盖 D 的场景。
- **出参契约（2026-09 统一，两套契约同形同字段）**：顶层固定为「三个回显标识 + `bscat_stats` + `bank_accounts` + `bscat_transactions` + `bscat_summaries`」。回显键**沿用入参拼写**（`userId` / `applicationId` / `flowTime`，v2 另加 `flow_id`），不再映射成 snake_case；`run_id` / `status` / `error` **只在 failed 出参里出现**——成功出参没有 run 信封（一次运行由回显的 `applicationId` + `flowTime` 标识）。`bank_accounts`（v1）= `bank_account_id` / `account_number` / `account_type` / `bank`（**去掉 `credit_limit`、补 `account_number`**），按 `bank_account_id` 去重后一账号一条（重复折叠为最后一条、无 id 条目丢弃；v2 同规则，按 `institution-account_number` 去重，但回显固定 8 键——`bsb` / `account_number` / `bank` / `institution` / `account_type` / `account_holder` / `account_holder_type` / `account_name`，条目没送的键出 null）。逐笔行 **不含** `application_id`；v1 行是它自己的固定字段集（`bank_account_id` / `transaction_id` 在内），v2 行 = 上游字段原顺序透传（原样回显，`""` / null / 数组都保持原值）+ `counterparty` / `bscat` / `stream_id`，**六个合成列都不出现**。summaries 两套契约列集完全一致：income_summary 14 列、liability_summary 12 列、category_summary 8 列——两个 stream summary 去掉 `bscat` / `bank_account_id` / `application_id` / `counterparty`，category_summary **保留 `bscat`**（按它聚合）、去掉 `bank_account_id`；v2 不再把 `bank_account_id` 反查成 `institution` + `bank_account_number`（反查代码已删）。失败出参 = `{status, error, bscat_stats, 三个回显键, bscat_transactions: [], bscat_summaries: {}}`（无 `bank_accounts`、无 `run_id`）；**失败不再只表示入参不合法**——引擎 / 序列化异常也走这个形状（见上「健壮性兜底」A），请求体不是 JSON 对象时没有回显键可取。
- **输出字段命名（2026-09 原生改名）**：分类结果列在全链路统一叫 `bscat`——内部 DataFrame 列、JSON 逐笔行、`bscat_summaries` 各子表、Excel 报告列、基线工件列，以及回溯/backfill 侧的 camelCase 键（原 `finvCategory` → `bscat`）；源码与已跟踪工件内不再有 `finv_category` 拼写（仅 gitignore 的 `backfill/output/**` 历史产物除外）。三个 JSON 出参键同源同名（`bscat_stats` / `bscat_transactions` / `bscat_summaries`），`bscat_summaries` 内部的 `income_summary` / `liability_summary` / `category_summary` 子键名**未变**；规则 CSV 用 `category` 列名，不受影响。顶层回显键 `userId` / `applicationId` / `flowTime` 是全链路唯一保留**入参驼峰拼写**的地方（因为就是原样回显）；其余出参键维持 snake_case。

- `backfill.py`：CSV 批量输入 → 运行流水线 → 输出 Excel 报告（默认 `output/classification_report_{YYYYMMDD_HHMMSS}.xlsx`，可用 `--output` 指定）。**不经过 JSON 契约层**（`pd.read_csv` → 直接 `orchestrator.run` → `write_report`，不调 `build_input_frame` / `serialize_output`），因此「两套 JSON 出入参自适应」对它不适用——它既不吃 illion JSON 也不吃 wagego JSON，CSV 需自带 `application_id` / `transaction_id` 等内部键列；要让 wagego 报文出批量 Excel 报告需另写 CSV 适配（当前没有）。
- `classification_traceback.py`：回溯工具（仓库根单文件），自建 v1 内部 frame，只 import v1 的 `build_transactions_frame` / `serialize_result`；派发层不介入，但**出参随 `serialize_result` 一起变成了新契约**（顶层回显驼峰键、无 run 信封、summary 列集收窄）。它自己的 `generate_classification` 只是把 `serialize_result` 的结果摊平后加 `engineClaims` / `engineStats` 两键，不读 `run_id` / `status`，故无需改代码。
- `verify_model.py`：单应用 JSON 输入（默认 `model_input.json`，可用 `--input` 指定）→ 运行流水线 → 输出 JSON（默认 `output/model_output_{application_id}_{时间戳}.json`，application_id 取 v1 的 `applicationId` 或 v2 的 `applicationId`/`flowId`，空则 `unknown`，文件名过滤 Windows 非法字符）。两套契约都支持，走 `build_input_frame` / `serialize_output` 派发；**保留 `orchestrator.run` 异常外抛**（本地脚本语义，与生产的 failed 出参不同），因此它只产出成功形状。序列化时排除引擎内部追踪列（`classification_status` 等），账户元数据（`account_type` / `bank` / `credit_limit`）只保留在内部帧、不进 JSON；出参字段见上面「出参契约」条。本质是单应用验证/试跑脚本。**注意：`model_main.py`（生产推理）与 `verify_model.py` 共用同一套构建/序列化（`classification_core/service.py` 的 `build_input_frame` / `serialize_output`），改输出字段结构等于改生产输出契约。**
- `check_wagego_sample.py`：v2 契约回归把手——跑匿名样例 `wagego_sample.json`（58 行，假姓名/假账号，生成脚本 `output/_make_wagego_sample.py`；真实样例 `wagego_example.json` 含 PII，已 gitignore），断言 `bscat_stats` / 成功出参无 run 信封 / 回显驼峰键 / 行键集合与顺序（**不含 `transaction_id`**）/ 行内上游值原样回显（`""` 仍是 `""`、null 仍是 null）/ 不出现合成列 / `bank_accounts` 回显固定 8 键（`bank` 在内）/ `bscat_summaries` 三张表的列集与「无账号身份」/ 各类别笔数快照 / **product 派发**（注册值照常路由、未注册值 failed 且回显原值、`product` 压过按键判定）/ **账号去重**（重复账号不扇行、回显折叠为最后一条、无 id 条目被丢弃）/ **`account_number` 单账号形态**（行不带 institution 时仍解析出账号元数据与 `bank`，分类结果不变）。**改 v2 适配层或序列化后除了 `baseline.py diff` 还要跑它**（`baseline.py` 只覆盖 v1）。
- `baseline.py`：基线回归对比（见下节）。
- `input_converter.py`：从 `sample.csv` 提取指定 application（交互式选择），转换为 `verify_model.py` 的 JSON 入参结构（v1，`--input` / `--output-dir`）。

## 基线回归对比（output 变更检查）

- **每次改完代码必须运行** `python baseline.py diff`（用 `.venv-backfill/Scripts/python.exe`，本机无 `.venv`；Python 3.13.14），将当前流水线输出与基线对比，检查分类结果是否变更。基线为**四层**，由 `save` 一次性生成，缺一不可：
  - `baseline/sample_baseline.csv`（最终输出层）：每笔交易 1 行，只记最终赢家结果（`bscat` / `counterparty` / `classification_engine` / `stream_id`）。
  - `baseline/engine_claims.csv`（每引擎认领层）：每引擎每交易 1 行，记录各引擎的认领（含 `classification_rule_id` / `classification_reason` / `stream_id` / `priority`），无论该行最终被哪台引擎赢走。**只有它才能抓到"引擎改了逻辑但没当上最终赢家"的回归**（此类变更在最终层完全不可见）。数据来自 `orchestrator.py` 的 `_archive_claims`，挂在 `EngineExecution.claims` 上。
  - `baseline/run_meta.json`（配置/版本层）：引擎清单（engine_id / priority / enabled）、`on_engine_error`、各引擎 `engine_version`、`baseline_format_version`，以及**输入文件与全部规则资源的 SHA-256 指纹**（各引擎 `resources/` 下的 CSV 与 `initial_engine/merchant_kb.csv`）。用于抓取**行级对比看不到的变更**——例如只调换 priority 而结果恰好相同、引擎启停/增删、引擎版本号被改，或改动了规则 CSV/输入样本。
  - `baseline/summaries/`（汇总指标层）：只比对确定性指标——`category_summary.csv` 全列（最终输出层的分类聚合统计）、`liability_summary.csv` 仅金额列（`funded_amount` / `repaid_amount` / `repayment_amount` / `recent_fn_repay_amount`）。时间敏感字段（`status` / `predicted_closing_date` / `frequency` 等）**不纳入**，避免换样本或日期推移导致的噪音误报。两个工件都按键列（application_id / bank_account_id / bscat / stream_id）对齐；`category_summary` 存在同键多行时保留首行（提取与加载两层都去重）。
- `diff` 输出差异：最终输出变化（CHANGED / NEW / GONE）+ 引擎认领变化（含 rule_id 计数增减）+ 汇总指标变化 + 配置/版本变化 + 行数检查（输入交易数或认领数增减）。以上任何一类有差异都 exit 1（含**规则认领计数变化**——改规则 CSV 后最常见的信号）。若 `diff` 报有差异，必须逐笔向用户说明差异原因（对应改动的规则/逻辑），不能只报告"有差异"就结束。
- 基线由 `python baseline.py save` 生成（会同时生成所有文件）。若任何基线工件已存在，命令会拒绝覆盖（exit 2）；仅当用户确认分类结果变更符合预期时，才允许用 `python baseline.py save --replace --reason "<确认原因>"` 明确重建。`diff` 会报告输入/规则文件的 SHA-256 指纹变化与 `baseline_format_version` 变化。
- 用 `.venv-backfill/Scripts/python.exe` 环境运行（`baseline.py` 输出已英文化，Windows 控制台无需再设置 `PYTHONIOENCODING`）。

## 重要约定

### 引擎相关改动需先确认

**所有涉及引擎顺序调整的改动（新增引擎、删除引擎、调整 priority、启用/禁用引擎、修改覆盖规则），都必须先与用户确认后再实施。**删除基线文件或修改 `configs/` 下配置文件同理，均属不可逆操作，需先确认。

### 规则数据外置

引擎的具体规则大多外置在各引擎 `resources/` 目录的 CSV 中（如 `liability_engine/resources/`、`transfer_engine/resources/`、`catch_all_engine/resources/`），引擎代码只负责加载与执行。`initial_engine/merchant_kb.csv` 为商户知识库（已跟踪进 git，非 gitignore），**不含 Rent 类**（2026-09 已迁至 `rent_engine/resources/rent_rules.csv`）**与 Gambling 类**（2026-09 已迁至 `gambling_engine/resources/gambling_rules.csv` 的 `source=institution` 行）。`rent_rules.csv` / `gambling_rules.csv` 结构相同，均混合两类行：`source=rule`（关键词/正则规则，`counterparty` 为空 → 输出 "-"）与 `source=institution`（机构行：`rule_name`=商户名、`pattern` 为竖线分隔的关键词、`confidence`=0.95、`counterparty`=商户名）；加新机构时在表尾追加即可，无需改引擎代码。两个引擎的机构层共用 `classification_core/merchant_institution.py`（加载/自动机构建/整词匹配/让路判定）。改规则优先改 CSV，避免动引擎代码；各 CSV 的列约定见对应引擎的 `domain/` 加载函数与 docstring。注意：改动这些资源文件会改变 SHA-256 指纹，`baseline.py diff` 会报告，但行级结果未必变。

### 双仓库发布流程（GitHub 开发 → GitLab 生产）

- **GitHub（https://github.com/EliamZhang/ServiFlow-AI.git）是个人开发仓库**，日常开发、分支、合并都在这里进行。
- **GitLab（http://git.ppdaicorp.com/intl/risk/AUS/Model/AUS_BS_CAT_Model.git）是线上生产环境**，发布目标分支为 `main` / `release-prep` / `staging`（三个分支通常保持同步）。
- **标准发布顺序：先在 GitHub 完成开发与合并，确认无误后再推送到 GitLab**。不要跳过 GitHub 直接把本地改动推上 GitLab。
- 推送 GitLab 属于影响生产的不可逆操作，**必须先将改动在 GitHub 侧定稿（合并到 main 或至少推上 staging）并经用户确认后**才能推送。

### 协作方式

- 在开始执行前，先检查问题中是否存在错误前提、逻辑跳跃、信息缺失或目标不明确；

* 保持独立判断，不盲目认同或迎合我的观点；
* 明确区分 **已确认事实、合理推测、主观判断和未知信息** ；
* 改代码过程中有存疑问题时，可以向我询问
* 涉及数字、人物、政策、时间及关键结论时，优先核实可靠来源，并说明依据；
* 发现我的判断可能有误时，直接指出，并说明理由、潜在风险及其他可能的解释；
* 主动告诉我可能忽略的变量、边界条件、隐性成本、偏差和长期影响；
* 存在多种方案时，说明各方案的优缺点，并给出明确建议，而不是只罗列选项；
* 信息不足时不要编造；可以基于合理假设继续，但必须明确标注假设；
* 默认先给出结论和建议，再补充分析过程与依据；
* 可以自主进行分析、修改和本地验证，但涉及**提交代码、推送远程仓库、发送消息、发布内容、删除数据或其他不可逆操作**时，必须先征得我的明确确认。
